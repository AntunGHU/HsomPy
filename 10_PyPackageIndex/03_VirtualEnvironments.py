# 4'04

# standardni nacin kreiranja venva
# ? > python3 -m venv env
# u root-u se stvara mapa "env" sa podmapama i "pyenv.cfg" fajlom. Podmape: bin, iclude, lib, lib64, share
# aktiviranje "source env/bin/activate" a na win: env\Sources\activate.bat
# u oba slucaja pojavljuje se (env) znak aktiviranog env-a
# instaliramo lokalno verziju requests~=2.14.0
# dektiviranje sa
# ? > deactivate
# nestaje (env)
