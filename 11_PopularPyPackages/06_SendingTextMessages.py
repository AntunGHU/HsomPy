# 6'02

# Ovdje bi smo trebali nauciti slati text-poruke sa Twilio
# Imaju i library koji se nalazi na PyPI i koji nam omogucava rad na "visem" levelu zahvaljujuci sto se njihov paket na kraju pojavljuje kao Wraper!
# treba se prvo registrirati i to mi se ne da! zurim u susret Djangu-by-Mash
# sve to Ath realizira zapocinjuci novi projekt
# kde:
# ? > mkdir PyText
# ? > cd PyText
# ? PyText $ code .
# ? PyText $ pipenv install twilio
# kreiramo app.py u root-u projekta
# ulazak u virtual env sa 1. ili klikom na status bar i odabir ili 2. csp  pa  "python interpreter" klik na "Select Inerpreter" i klik na "(PyText)" 
# ssad dalje u app.py-i
# ? from twilio.rest import Client
# ? kreira Client object itd...