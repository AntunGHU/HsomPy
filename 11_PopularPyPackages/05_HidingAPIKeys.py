# 2'05

# Ath nastavlja i sakriva od git-a neke podatke i ja se pridruzujem!
# kreira fajl "config.py" i sjece API podatak iz app.py-a (unutar kog radi aplikaciju projekta AJPYYELP) i pasta ga u config.py pa se vraca u app.py i na vrh dodaje liniju 
# ? import config 
# te u liniji u kojoj je referencirao prije "api_key" sad dodaje config. tako da refernca sad je "config.api_key"
# sad kako bi iskljucio config.py iz git-a kreira fajl .gitignore (bez ekstenzije)
# upisujemo u gitignoore ime fajla sa ekstenzijama
# ja upisao "config.py" ali i "Yelp.png" i "yelp.txt"